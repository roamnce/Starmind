# Starmind 领域术语表

本文定义项目内通用的领域术语，确保 Flutter 和 Rust 两侧的代码与文档用语一致。

## Document（文档）

用户导入 Starmind 的 PDF 书籍、论文或阅读材料。导入时文件会被拷贝到应用沙盒存储目录，并在 SQLite 中建立一条记录——包含唯一 ID、标题、文件路径和所属 Folder ID。

1. 一个 Document 最多属于一个 Folder，没有 Folder 的即为**未分类**。
2. 一个 Document 可以关联多个 Tag。

## Folder（文件夹）

分层容器，用来归类 Document。通过 `parent_id` 实现树形嵌套——即文件夹可以无限嵌套子文件夹。删除 Folder 时会弹出提示，让用户选择**保留文档**（移至"未分类"）还是**级联删除**（连同子文件夹一起移入回收站）。

## Tag（标签）

跨维度元数据标签，目前挂在 Document 上，后续会扩展到摘录卡片。同样通过 `parent_id` 构成树形层级，如"学习 → 编程 → Flutter"。每个 Tag 可以带自定义样式（如 `color_hex`）。

1. 一个 Document 可关联多个 Tag（多对多关系）。
2. 一个 Tag 也可以挂在多个 Document 上。

## Workspace（工作区）

用户管理和阅读材料的多标签页容器。标签页列表中第一个永远是不可关闭的**首页**，后续可以打开多个 Document 标签页。工作区追踪当前活跃标签页索引和浏览历史。

## Split Panel（分屏面板）

工作区 UI 的结构布局元素，支持递归的纵向或横向分屏。用树形结构表示——每个节点要么是 Parent（把空间分成水平/垂直子区域），要么是 Leaf（渲染标签栏和当前页面）。拖拽标签到另一个面板即可触发分屏，支持任意并排组合，如首页+PDF、PDF+PDF、PDF+思维导图。

## 首页

Workspace 的默认视图，展示笔记和 PDF 卡片列表、左侧文件夹树与标签分类。

## PDF 批注视图

激活 PDF 标签页时的视图。默认不显示侧边栏，只保留顶部标签栏，把大部分空间留给 PDF 渲染区。

## 沉浸模式

PDF 批注视图特有的全屏状态。开启后标签栏隐藏，实现沉浸式阅读与批注。

## 侧边栏

首页左侧的文件夹树与标签列表，可折叠/展开。

## 摘录栏

PDF 批注视图右侧的面板，展示用户从 PDF 中高亮提取的文本摘录及思维导图。

## 底部工具栏

PDF 批注视图底部浮动控制条——左侧显示页码与总页数，右侧有缩放按钮和沉浸模式开关。

## PDF 视口

PDF 批注视图中的核心视口组件。用于呈现 PDF 页面内容，并通过矩阵变换实现平滑的捏合缩放（Pinch-to-Zoom）与任意方向的拖拽平移（Pan）。所有页面在 Flutter 布局树中采用固定的静态尺寸（不随缩放动态重排），缩放纯粹由图形变换层处理。

## 高清切片渲染

视口缩放与滚动停止后触发的渲染机制。系统仅针对当前视口中可见的 PDF 局部区域进行高分辨率的切片渲染，并将其作为局部贴图覆盖在原有的低清整页预览图上，以实现清晰的字迹呈现和极低的渲染内存占用。

## 手势路由

在手写墨迹模式下，对用户触摸事件进行智能路由分发的机制。单指或触控笔的单点移动会被路由至手写层进行墨迹绘制；而双指或触控笔防误触模式下的手部压屏，则会被智能路由至视口变换逻辑，从而实现无需切换工具即可随时平移与缩放。