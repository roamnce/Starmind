# v16.4 — Markdown WYSIWYG 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 LivePreviewController 单 TextField 架构上，补齐思源笔记级别的 Markdown 渲染效果和交互能力。

**Architecture:** 混合渲染方案 — TextField 内用透明占位符保持长度不变量，Stack Overlay 层在对应位置渲染真实 widget（图片、代码块高亮、表格、数学公式、Mermaid图表）。

**Tech Stack:** Flutter 3.x, Dart 3.x, `flutter_highlight` (语法高亮), `flutter_math_fork` (LaTeX公式), `file_picker` (附件插入), `url_launcher` (链接打开), `path_provider` (文件管理)

**Spec:** `docs/v16.4/v16.4-spec.md`

---

## 依赖项准备

### Task 0: 添加 pubspec.yaml 依赖

**Files:**
- Modify: `pubspec.yaml`

- [ ] **Step 1: 添加依赖包**

```yaml
# 在 dependencies: 块中添加：
  flutter_highlight: ^0.7.0
  highlight: ^0.7.0
  flutter_math_fork: ^0.7.2
  url_launcher: ^6.3.1
```

- [ ] **Step 2: 运行 flutter pub get**

```bash
cd D:/starmind && flutter pub get
```

Expected: `Process finished with exit code 0`

- [ ] **Step 3: 创建 v16.4 目录结构**

```bash
mkdir -p D:/starmind/docs/v16.4/specs
mkdir -p D:/starmind/lib/src/mindmap/ui/cards/live_preview/rendering
```

- [ ] **Step 4: 验证当前测试可用**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/ 2>/dev/null || echo "无现有live_preview测试"
```

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add pubspec.yaml docs/v16.4/ && git commit -m "$(cat <<'EOF'
build(deps): add highlight, flutter_math_fork, url_launcher for v16.4 WYSIWYG rendering

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 1 — 图片内联渲染 + 插入

### Task 1: PlaceholderTracker — 占位符管理

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart`
- Test: `test/mindmap/ui/cards/live_preview/placeholder_tracker_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/placeholder_tracker_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart';

void main() {
  group('PlaceholderTracker', () {
    test('rebuild detects image placeholders', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('text ![alt](path/to/image.png) more text');
      expect(tracker.placeholders.length, 1);
      expect(tracker.placeholders.first.type, 'image');
      expect(tracker.placeholders.first.rawMarkdown, '![alt](path/to/image.png)');
    });

    test('rebuild detects multiple placeholders', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('![a](1.png) text ![b](2.png)');
      expect(tracker.placeholders.length, 2);
    });

    test('rebuild with no placeholders returns empty', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('plain text without images');
      expect(tracker.placeholders.isEmpty, true);
    });

    test('findAtOffset returns correct placeholder', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('prefix ![img](a.png) suffix');
      // 'prefix ' = 7 chars, '![img](a.png)' = 13 chars
      final found = tracker.findAtOffset(8);
      expect(found, isNotNull);
      expect(found!.type, 'image');
    });

    test('findAtOffset returns null for non-placeholder offset', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('prefix ![img](a.png) suffix');
      final found = tracker.findAtOffset(2);
      expect(found, isNull);
    });

    test('totalPlaceholderChars sums all placeholder lengths', () {
      final tracker = PlaceholderTracker();
      tracker.rebuild('![a](x.png)![b](y.png)');
      // Each '![a](x.png)' is 10 chars -> 20 total
      expect(tracker.totalPlaceholderChars(), 20);
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/placeholder_tracker_test.dart -v
```
Expected: FAIL — `placeholder_tracker.dart` 不存在

- [ ] **Step 3: 实现 PlaceholderTracker**

```dart
// lib/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart
import 'package:flutter/material.dart';

/// 占位符信息，描述 TextField 中哪个字符范围被占位符替代。
class PlaceholderInfo {
  final String id;
  final int lineIndex;
  final int charOffset;
  final int length;
  final String type;
  final String rawMarkdown;

  const PlaceholderInfo({
    required this.id,
    required this.lineIndex,
    required this.charOffset,
    required this.length,
    required this.type,
    required this.rawMarkdown,
  });
}

/// 管理 TextField 内占位符 ↔ 真实 widget 的位置映射。
///
/// 每次 markdown 文本变更时调用 [rebuild] 重新解析，发现占位符。
/// [findAtOffset] 用于检测光标是否落在占位符范围内。
class PlaceholderTracker {
  final List<PlaceholderInfo> _placeholders = [];

  List<PlaceholderInfo> get placeholders => List.unmodifiable(_placeholders);

  void rebuild(String markdown) {
    _placeholders.clear();
    if (markdown.isEmpty) return;

    final lines = markdown.split('\n');
    var lineOffset = 0;
    for (var i = 0; i < lines.length; i++) {
      _scanLineForPlaceholders(lines[i], i, lineOffset);
      lineOffset += lines[i].length + 1;
    }
  }

  void _scanLineForPlaceholders(String line, int lineIndex, int lineOffset) {
    // 图片: ![alt](url)
    final imageRe = RegExp(r'!\[([^\]]*)\]\(([^)]+)\)');
    for (final m in imageRe.allMatches(line)) {
      _placeholders.add(PlaceholderInfo(
        id: '${lineIndex}_${m.start}',
        lineIndex: lineIndex,
        charOffset: lineOffset + m.start,
        length: m.end - m.start,
        type: 'image',
        rawMarkdown: m.group(0)!,
      ));
    }

    // 围栏代码块 (``` 或 ~~~)
    // 由外部驱动，此处只做单行检测
    final fenceRe = RegExp(r'^(```|~~~)(\w*)');
    if (fenceRe.hasMatch(line.trim())) {
      // 仅标记围栏起始行，完整代码块范围由外部调用者确定
    }

    // 块级公式: $$...$$
    final blockMathRe = RegExp(r'^\$\$[^$]+\$\$$');
    final blockMathMatch = blockMathRe.firstMatch(line.trim());
    if (blockMathMatch != null) {
      _placeholders.add(PlaceholderInfo(
        id: '${lineIndex}_${blockMathMatch.start}',
        lineIndex: lineIndex,
        charOffset: lineOffset + blockMathMatch.start,
        length: blockMathMatch.end - blockMathMatch.start,
        type: 'math',
        rawMarkdown: line,
      ));
    }
  }

  PlaceholderInfo? findAtOffset(int absoluteOffset) {
    for (final p in _placeholders) {
      if (absoluteOffset >= p.charOffset &&
          absoluteOffset < p.charOffset + p.length) {
        return p;
      }
    }
    return null;
  }

  int totalPlaceholderChars() {
    int total = 0;
    for (final p in _placeholders) {
      total += p.length;
    }
    return total;
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/placeholder_tracker_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart test/mindmap/ui/cards/live_preview/placeholder_tracker_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add PlaceholderTracker for overlay widget position mapping

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: ImageRenderer — 图片渲染 Widget

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/rendering/image_renderer.dart`
- Test: `test/mindmap/ui/cards/live_preview/image_renderer_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/image_renderer_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/rendering/image_renderer.dart';

void main() {
  group('ImageRenderer', () {
    testWidgets('renders error widget for invalid path', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: ImageRenderer(
            imagePath: '/nonexistent/image.png',
            maxWidth: 300,
          ),
        ),
      );
      await tester.pump();
      expect(find.text('[图片加载失败]'), findsOneWidget);
    });

    testWidgets('renders Image.file for valid path', (tester) async {
      // 仅验证 widget 树中包含 Image widget
      await tester.pumpWidget(
        MaterialApp(
          home: ImageRenderer(
            imagePath: '/nonexistent/image.png',
            maxWidth: 300,
          ),
        ),
      );
      await tester.pump();
      // 即使路径不存在也应有 Image widget（只是显示错误）
      expect(find.byType(Image), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/image_renderer_test.dart -v
```
Expected: FAIL

- [ ] **Step 3: 实现 ImageRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/rendering/image_renderer.dart
import 'dart:io';
import 'package:flutter/material.dart';

/// 将本地图片渲染为带最大宽度约束的 widget。
///
/// 加载失败时显示 `[图片加载失败]` 文本。
/// 图片等比缩放，最大高度 400px。
class ImageRenderer extends StatefulWidget {
  final String imagePath;
  final double maxWidth;

  const ImageRenderer({
    super.key,
    required this.imagePath,
    required this.maxWidth,
  });

  @override
  State<ImageRenderer> createState() => _ImageRendererState();
}

class _ImageRendererState extends State<ImageRenderer> {
  File? _file;
  bool _hasError = false;

  @override
  void initState() {
    super.initState();
    _validateFile();
  }

  @override
  void didUpdateWidget(ImageRenderer oldWidget) {
    super.didUpdateWidget(oldWidget);
    if (oldWidget.imagePath != widget.imagePath) {
      _hasError = false;
      _validateFile();
    }
  }

  void _validateFile() {
    final file = File(widget.imagePath);
    if (file.existsSync()) {
      _file = file;
    } else {
      _file = null;
      _hasError = true;
    }
  }

  @override
  Widget build(BuildContext context) {
    if (_hasError || _file == null) {
      return const Padding(
        padding: EdgeInsets.symmetric(vertical: 4),
        child: Text(
          '[图片加载失败]',
          style: TextStyle(color: Colors.red, fontSize: 13),
        ),
      );
    }

    return ConstrainedBox(
      constraints: BoxConstraints(
        maxWidth: widget.maxWidth,
        maxHeight: 400,
      ),
      child: ClipRRect(
        borderRadius: BorderRadius.circular(4),
        child: Image.file(
          _file!,
          fit: BoxFit.contain,
          errorBuilder: (context, error, stackTrace) {
            return const Text(
              '[图片加载失败]',
              style: TextStyle(color: Colors.red, fontSize: 13),
            );
          },
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/image_renderer_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/rendering/image_renderer.dart test/mindmap/ui/cards/live_preview/image_renderer_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add ImageRenderer for local image display in cards

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: FileAttachmentService — 文件/附件管理

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/file_attachment_service.dart`
- Test: `test/mindmap/ui/cards/live_preview/file_attachment_service_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/file_attachment_service_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/file_attachment_service.dart';

void main() {
  group('FileAttachmentService', () {
    test('isImageFile returns true for image extensions', () {
      expect(FileAttachmentService.isImageFile('photo.png'), true);
      expect(FileAttachmentService.isImageFile('photo.jpg'), true);
      expect(FileAttachmentService.isImageFile('photo.jpeg'), true);
      expect(FileAttachmentService.isImageFile('photo.gif'), true);
      expect(FileAttachmentService.isImageFile('photo.webp'), true);
      expect(FileAttachmentService.isImageFile('photo.bmp'), true);
    });

    test('isImageFile returns false for non-image extensions', () {
      expect(FileAttachmentService.isImageFile('document.pdf'), false);
      expect(FileAttachmentService.isImageFile('notes.txt'), false);
      expect(FileAttachmentService.isImageFile('file.md'), false);
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/file_attachment_service_test.dart -v
```
Expected: FAIL

- [ ] **Step 3: 实现 FileAttachmentService**

```dart
// lib/src/mindmap/ui/cards/live_preview/file_attachment_service.dart
import 'dart:io';
import 'package:path/path.dart' as p;
import 'package:path_provider/path_provider.dart';
import 'package:uuid/uuid.dart';

/// 管理和插入文件附件（图片、文档等）。
///
/// 支持:
/// - 判断文件类型（图片 vs 其他）
/// - 将文件复制到应用资产目录
/// - 生成 markdown 插入文本（图片用 ![]()，其他用 []()）
class FileAttachmentService {
  static const _imageExtensions = {
    '.png', '.jpg', '.jpeg', '.gif', '.webp', '.bmp',
  };

  /// 判断文件是否是图片类型。
  static bool isImageFile(String fileName) {
    final ext = p.extension(fileName).toLowerCase();
    return _imageExtensions.contains(ext);
  }

  /// 将源文件复制到 assets 目录中，返回新路径。
  ///
  /// 图片放入 assets/images/，其他文件放入 assets/files/。
  static Future<String> copyToAssets(String sourcePath) async {
    final appDir = await getApplicationDocumentsDirectory();
    final sourceFile = File(sourcePath);
    final ext = p.extension(sourcePath);
    final baseName = p.basenameWithoutExtension(sourcePath);
    final uuid = const Uuid().v4();

    String targetDir;
    String newName;
    if (isImageFile(sourcePath)) {
      targetDir = p.join(appDir.path, 'assets', 'images');
      newName = '${baseName}_$uuid$ext';
    } else {
      targetDir = p.join(appDir.path, 'assets', 'files');
      newName = '${uuid}_$baseName$ext';
    }

    await Directory(targetDir).create(recursive: true);
    final targetPath = p.join(targetDir, newName);
    await sourceFile.copy(targetPath);
    return targetPath;
  }

  /// 生成 markdown 插入文本。
  ///
  /// 图片返回 `![alt](path)`，其他文件返回 `[name](path)`。
  static String toMarkdownInsertion(String filePath, {String? alt}) {
    final fileName = p.basename(filePath);
    if (isImageFile(filePath)) {
      final label = alt ?? fileName;
      return '![$label]($filePath)';
    }
    return '[$fileName]($filePath)';
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/file_attachment_service_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/file_attachment_service.dart test/mindmap/ui/cards/live_preview/file_attachment_service_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add FileAttachmentService for image/file asset management

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: CardOverlayRenderer — Overlay 层渲染

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart`
- Test: `test/mindmap/ui/cards/live_preview/card_overlay_renderer_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/card_overlay_renderer_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart';

void main() {
  group('CardOverlayRenderer', () {
    testWidgets('renders nothing when no placeholders', (tester) async {
      final tracker = PlaceholderTracker();
      tracker.rebuild('no placeholders here');
      await tester.pumpWidget(
        MaterialApp(
          home: SizedBox(
            width: 400,
            height: 600,
            child: CardOverlayRenderer(
              markdown: 'no placeholders here',
              maxWidth: 380,
              placeholderTracker: tracker,
              textFieldKey: GlobalKey(),
            ),
          ),
        ),
      );
      await tester.pump();
      // 无占位符时应该是个空的 SizedBox
      expect(find.byType(CardOverlayRenderer), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/card_overlay_renderer_test.dart -v
```
Expected: FAIL

- [ ] **Step 3: 实现 CardOverlayRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart
import 'package:flutter/material.dart';
import 'placeholder_tracker.dart';
import 'rendering/image_renderer.dart';

/// 在 TextField 上方叠加渲染真实 widget（图片、代码块等）。
///
/// 读取 [PlaceholderTracker] 找出所有占位符位置，在对应位置渲染 widget。
/// 位置通过 [GlobalKey] 获取 TextField 中每行的 RenderBox 计算。
class CardOverlayRenderer extends StatelessWidget {
  final String markdown;
  final double maxWidth;
  final PlaceholderTracker placeholderTracker;
  final GlobalKey textFieldKey;

  const CardOverlayRenderer({
    super.key,
    required this.markdown,
    required this.maxWidth,
    required this.placeholderTracker,
    required this.textFieldKey,
  });

  @override
  Widget build(BuildContext context) {
    final placeholders = placeholderTracker.placeholders;
    if (placeholders.isEmpty) return const SizedBox.shrink();

    // 从 TextField 的 RenderBox 计算占位符位置
    final textFieldRenderBox =
        textFieldKey.currentContext?.findRenderObject() as RenderBox?;
    if (textFieldRenderBox == null) return const SizedBox.shrink();

    final widgets = <Widget>[];

    for (final p in placeholders) {
      switch (p.type) {
        case 'image':
          final url = _extractUrl(p.rawMarkdown);
          if (url != null) {
            widgets.add(
              Positioned(
                left: 8,
                top: _estimateLineTop(textFieldRenderBox, p.lineIndex),
                right: 8,
                child: ImageRenderer(
                  imagePath: url,
                  maxWidth: maxWidth,
                ),
              ),
            );
          }
      }
    }

    if (widgets.isEmpty) return const SizedBox.shrink();

    return Stack(children: widgets);
  }

  /// 从 `![alt](url)` 提取 url。
  String? _extractUrl(String rawMarkdown) {
    final re = RegExp(r'!\[([^\]]*)\]\(([^)]+)\)');
    final m = re.firstMatch(rawMarkdown);
    return m?.group(2);
  }

  /// 估算指定行在 TextField 中的垂直位置。
  ///
  /// 基于行号 * 行高来估算。精确位置需要 TextField 的布局信息。
  double _estimateLineTop(RenderBox textFieldRenderBox, int lineIndex) {
    // 近似：每行约 24px（含行间距）
    return 8.0 + lineIndex * 24.0;
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/card_overlay_renderer_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart test/mindmap/ui/cards/live_preview/card_overlay_renderer_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add CardOverlayRenderer for widget overlay on text field

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: LivePreviewController 图片占位符 + CardBody Stack 集成

**Files:**
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_controller.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_card_body.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_toolbar.dart`

- [ ] **Step 1: 修改 LivePreviewController — 图片占位符注入**

在 `live_preview_controller.dart` 的 `_emitInlineSpan` 中，修改 `_InlineKind.image` 分支：

```dart
case _InlineKind.image:
  // 图片占位：用布莱叶盲文空格保持长度不变量
  final rawMatch = match.group(0)!;
  out.add(TextSpan(
    text: rawMatch,
    style: const TextStyle(color: Color(0x00000000)),
  ));
```

- [ ] **Step 2: 修改 LivePreviewCardBody — Stack 包装**

将 Column 结构改为 Stack：

```dart
// 替换原有的 return Column(...)
return Stack(
  children: [
    Column(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        if (widget.showToolbar && !widget.readOnly)
          Padding(
            padding: const EdgeInsets.fromLTRB(8, 6, 8, 12),
            child: LivePreviewToolbar(
              onAction: _handleToolbarAction,
              iconColor: text.withValues(alpha: 0.85),
            ),
          ),
        Expanded(child: editor),
      ],
    ),
    // Overlay 层——渲染图片等 widget
    CardOverlayRenderer(
      markdown: _controller.text,
      maxWidth: MediaQuery.of(context).size.width - 40,
      placeholderTracker: _placeholderTracker,
      textFieldKey: _textFieldKey,
    ),
  ],
);
```

并添加状态变量：
```dart
// 在 LivePreviewCardBodyState 类中添加
final _placeholderTracker = PlaceholderTracker();
final _textFieldKey = GlobalKey();
// 并将 TextField 的 key 设为 _textFieldKey
```

- [ ] **Step 3: 修改 Toolbar — 图片插入走 file_picker**

修改 `_handleToolbarAction` 中 `MdToolbarAction.image` 分支：

```dart
case MdToolbarAction.image:
  _pickAndInsertImage();
  break;
```

添加 `_pickAndInsertImage` 方法：

```dart
Future<void> _pickAndInsertImage() async {
  try {
    final result = await FilePicker.platform.pickFiles(
      type: FileType.custom,
      allowedExtensions: ['png', 'jpg', 'jpeg', 'gif', 'webp', 'bmp'],
      allowMultiple: false,
    );
    if (result == null || result.files.isEmpty) return;
    final filePath = result.files.first.path;
    if (filePath == null) return;

    // 复制到 assets
    final assetPath = await FileAttachmentService.copyToAssets(filePath);
    final alt = result.files.first.name;
    final markdown = FileAttachmentService.toMarkdownInsertion(
      assetPath,
      alt: alt,
    );
    _insertAtSelection(markdown);
  } catch (e) {
    debugPrint('Image pick error: $e');
  }
}
```

- [ ] **Step 4: 运行现有测试验证未回归**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/ -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): integrate image placeholder rendering with Stack overlay

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 2 — 链接可点击 + 附件插入

### Task 6: LinkHandler — Ctrl+Click 链接打开

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/link_handler.dart`
- Test: `test/mindmap/ui/cards/live_preview/link_handler_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/link_handler_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/link_handler.dart';

void main() {
  group('LinkHandler', () {
    test('extractLinks finds [text](url) patterns', () {
      final links = LinkHandler.extractLinks('see [example](https://example.com) for details');
      expect(links.length, 1);
      expect(links.first.text, 'example');
      expect(links.first.url, 'https://example.com');
    });

    test('extractLinks returns empty for plain text', () {
      final links = LinkHandler.extractLinks('plain text without links');
      expect(links.isEmpty, true);
    });

    test('isLinkAtOffset detects cursor on link', () {
      final links = LinkHandler.extractLinks('[test](http://x.com)');
      // [ = offset 0, t = 1, e = 2, s = 3, t = 4, ] = 5
      // text 部分的偏移: 1-4
      expect(LinkHandler.isLinkAtOffset(links, 1), isNotNull); // 't'
      expect(LinkHandler.isLinkAtOffset(links, 4), isNotNull); // 't'
      expect(LinkHandler.isLinkAtOffset(links, 0), isNull);    // '['
      expect(LinkHandler.isLinkAtOffset(links, 5), isNull);    // ']'
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/link_handler_test.dart -v
```
Expected: FAIL

- [ ] **Step 3: 实现 LinkHandler**

```dart
// lib/src/mindmap/ui/cards/live_preview/link_handler.dart
import 'package:flutter/material.dart';
import 'package:url_launcher/url_launcher.dart';

/// 解析和处理 markdown 链接的结构体。
class LinkInfo {
  final String text;
  final String url;
  final int startOffset;
  final int endOffset;

  const LinkInfo({
    required this.text,
    required this.url,
    required this.startOffset,
    required this.endOffset,
  });
}

/// 处理 markdown 链接的点击（Ctrl+Click）和解析。
class LinkHandler {
  /// 从文本中提取所有 [text](url) 链接。
  static List<LinkInfo> extractLinks(String text) {
    final links = <LinkInfo>[];
    final re = RegExp(r'\[([^\]]+)\]\(([^)]+)\)');
    for (final m in re.allMatches(text)) {
      links.add(LinkInfo(
        text: m.group(1)!,
        url: m.group(2)!,
        startOffset: m.start,
        endOffset: m.end,
      ));
    }
    return links;
  }

  /// 判断指定字符偏移是否落在某个链接范围内。
  ///
  /// 返回命中的 [LinkInfo]，未命中返回 null。
  static LinkInfo? isLinkAtOffset(List<LinkInfo> links, int offset) {
    for (final link in links) {
      // `[text](url)` — 方括号起始到闭合括号结束
      if (offset >= link.startOffset && offset < link.endOffset) {
        return link;
      }
    }
    return null;
  }

  /// 打开链接。
  ///
  /// 网络 URL 使用 url_launcher，本地路径使用 File/Process。
  static Future<void> openLink(String url) async {
    final uri = Uri.tryParse(url);
    if (uri == null) return;

    if (url.startsWith('http://') || url.startsWith('https://')) {
      await launchUrl(uri, mode: LaunchMode.externalApplication);
    } else {
      // 本地文件 — 由操作系统打开
      await launchUrl(uri, mode: LaunchMode.externalApplication);
    }
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/link_handler_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/link_handler.dart test/mindmap/ui/cards/live_preview/link_handler_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add LinkHandler for Ctrl+Click link opening

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: CardBody 集成链接点击 + 附件插入

**Files:**
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_card_body.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_toolbar.dart`

- [ ] **Step 1: 在 CardBody 中添加 Ctrl+Click 链接处理**

在 `build()` 方法中，在 `TextField` 的 `onTap` 回调中增加 Ctrl 检测：

```dart
// 在 TextField 构造中:
onTap: () {
  _toggleCheckboxAtCursor();
  _handleLinkTap();
},

// 新增方法:
void _handleLinkTap() {
  final isCtrlPressed =
      HardwareKeyboard.instance.isControlPressed ||
      HardwareKeyboard.instance.isMetaPressed;
  if (!isCtrlPressed) return;

  final offset = _controller.selection.baseOffset;
  if (offset < 0 || offset > _controller.text.length) return;

  final links = LinkHandler.extractLinks(_controller.text);
  final link = LinkHandler.isLinkAtOffset(links, offset);
  if (link != null) {
    LinkHandler.openLink(link.url);
  }
}
```

- [ ] **Step 2: 工具栏新增"插入附件"按钮**

在 `live_preview_toolbar.dart` 中添加新的 enum 值和按钮：

```dart
// 在 MdToolbarAction 中增加:
attachFile,

// 在 _btn 列表中增加:
_btn(Icons.attach_file, '插入附件', MdToolbarAction.attachFile),
```

`live_preview_card_body.dart` 中处理：

```dart
case MdToolbarAction.attachFile:
  _pickAndInsertFile();
  break;

Future<void> _pickAndInsertFile() async {
  try {
    final result = await FilePicker.platform.pickFiles(
      type: FileType.any,
      allowMultiple: false,
    );
    if (result == null || result.files.isEmpty) return;
    final filePath = result.files.first.path;
    if (filePath == null) return;

    final assetPath = await FileAttachmentService.copyToAssets(filePath);
    final markdown = FileAttachmentService.toMarkdownInsertion(assetPath);
    _insertAtSelection(markdown);
  } catch (e) {
    debugPrint('File pick error: $e');
  }
}
```

- [ ] **Step 3: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/ -v
```
Expected: PASS

- [ ] **Step 4: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): add Ctrl+Click link opening and file attachment insertion

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 3 — 代码块 + 语法高亮

### Task 8: CodeBlockRenderer — 代码块语法高亮

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/rendering/code_block_renderer.dart`
- Test: `test/mindmap/ui/cards/live_preview/code_block_renderer_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/code_block_renderer_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/rendering/code_block_renderer.dart';

void main() {
  group('CodeBlockRenderer', () {
    testWidgets('renders code with monospace font', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: CodeBlockRenderer(
            code: 'void main() { print("hello"); }',
            language: 'dart',
            maxWidth: 400,
          ),
        ),
      );
      await tester.pump();
      expect(find.byType(Container), findsOneWidget);
    });

    testWidgets('renders language label when present', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: CodeBlockRenderer(
            code: 'console.log("hi");',
            language: 'javascript',
            maxWidth: 400,
          ),
        ),
      );
      await tester.pump();
      expect(find.text('javascript'), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: 确保 highlight 依赖已添加**

检查 `pubspec.yaml` 是否有 `flutter_highlight` 和 `highlight` 依赖。如果 Task 0 已完成则跳过。

```bash
cd D:/starmind && grep -c "flutter_highlight" pubspec.yaml
```

- [ ] **Step 3: 实现 CodeBlockRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/rendering/code_block_renderer.dart
import 'package:flutter/material.dart';
import 'package:highlight/languages/all.dart' show allLanguages;
import 'package:flutter_highlight/flutter_highlight.dart';
import 'package:flutter_highlight/themes/github.dart';

/// 渲染带语法高亮的代码块 widget。
///
/// 使用 [flutter_highlight] 包实现语法着色。
/// 深色背景 + 等宽字体 + 可选行号。
class CodeBlockRenderer extends StatelessWidget {
  final String code;
  final String? language;
  final double maxWidth;

  const CodeBlockRenderer({
    super.key,
    required this.code,
    this.language,
    required this.maxWidth,
  });

  @override
  Widget build(BuildContext context) {
    final lang = language?.toLowerCase() ?? '';
    final highlightTheme = githubTheme;

    return Container(
      width: maxWidth,
      margin: const EdgeInsets.symmetric(vertical: 4),
      decoration: BoxDecoration(
        color: const Color(0xFF1E1E1E),
        borderRadius: BorderRadius.circular(6),
        border: Border.all(color: const Color(0x33FFFFFF)),
      ),
      clipBehavior: Clip.antiAlias,
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        mainAxisSize: MainAxisSize.min,
        children: [
          // 语言标识栏
          if (lang.isNotEmpty)
            Container(
              width: double.infinity,
              padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
              decoration: BoxDecoration(
                color: const Color(0x1AFFFFFF),
                border: const Border(
                  bottom: BorderSide(color: Color(0x1AFFFFFF)),
                ),
              ),
              child: Text(
                lang,
                style: const TextStyle(
                  color: Color(0xFF888888),
                  fontSize: 11,
                  fontFamily: 'monospace',
                ),
              ),
            ),
          // 代码内容
          SingleChildScrollView(
            scrollDirection: Axis.horizontal,
            child: Padding(
              padding: const EdgeInsets.all(12),
              child: HighlightView(
                code,
                language: allLanguages.containsKey(lang) ? lang : 'plaintext',
                theme: highlightTheme,
                textStyle: const TextStyle(
                  fontFamily: 'monospace',
                  fontSize: 13,
                  height: 1.5,
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/code_block_renderer_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/rendering/code_block_renderer.dart test/mindmap/ui/cards/live_preview/code_block_renderer_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add CodeBlockRenderer with syntax highlighting

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: LivePreviewController 代码块占位符 + Overlay 集成

**Files:**
- Modify: `lib/src/mindmap/ui/cards/live_preview/live_preview_controller.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/placeholder_tracker.dart`

- [ ] **Step 1: 修改 PlaceholderTracker — 支持代码块范围检测**

在 `_scanLineForPlaceholders` 之后添加代码块扫描逻辑，或在外部用新方法驱动。添加：

```dart
/// 扫描完整 markdown 文本中的围栏代码块范围。
///
/// 检测 ```lang 和 ~~~lang 围栏，将整个代码块区域注册为占位符。
/// 需要在 [rebuild] 之后额外调用。
List<PlaceholderInfo> scanCodeFences(String markdown) {
  final fences = <PlaceholderInfo>[];
  final lines = markdown.split('\n');
  final fenceRe = RegExp(r'^(```|~~~)(\w*)');
  var inBlock = false;
  var blockStartLine = 0;
  var blockStartOffset = 0;
  var blockLang = '';
  var blockLines = <String>[];
  var lineOffset = 0;

  for (var i = 0; i < lines.length; i++) {
    final m = fenceRe.firstMatch(lines[i].trim());
    if (m != null && !inBlock) {
      inBlock = true;
      blockStartLine = i;
      blockStartOffset = lineOffset;
      blockLang = m.group(2) ?? '';
      blockLines = [lines[i]];
    } else if (m != null && inBlock) {
      // 闭合围栏
      inBlock = false;
      blockLines.add(lines[i]);
      final blockText = blockLines.join('\n');
      fences.add(PlaceholderInfo(
        id: 'code_${blockStartLine}',
        lineIndex: blockStartLine,
        charOffset: blockStartOffset,
        length: lineOffset + lines[i].length - blockStartOffset + 1,
        type: 'codeblock',
        rawMarkdown: blockText,
      ));
    } else if (inBlock) {
      blockLines.add(lines[i]);
    }
    lineOffset += lines[i].length + 1;
  }
  return fences;
}
```

- [ ] **Step 2: 修改 LivePreviewController — 代码块特殊渲染**

修改 `_renderLine` 中已有的围栏代码块逻辑：

```dart
// 围栏代码块起始行（``` 或 ~~~）
final fenceRe = RegExp(r'^(`{3,}|~{3,})(\w*)');
final fenceMatch = fenceRe.firstMatch(line);
if (fenceMatch != null) {
  final marker = fenceMatch.group(1)!;
  final lang = fenceMatch.group(2)!;
  // 当光标不在代码块范围内时，整行用透明占位符
  if (!revealSyntax) {
    out.add(TextSpan(
      text: line,
      style: const TextStyle(color: Color(0x00000000)),
    ));
  } else {
    out.add(TextSpan(text: marker, style: _syntaxStyle(true)));
    if (lang.isNotEmpty) {
      out.add(TextSpan(text: lang, style: TextStyle(
        fontFamily: 'monospace', color: codeColor, fontSize: 13,
      )));
    }
  }
  return;
}
```

- [ ] **Step 3: 修改 CardOverlayRenderer — 支持代码块渲染**

在 `build` 方法的 switch 中增加 `codeblock` 分支：

```dart
case 'codeblock':
  final lines = p.rawMarkdown.split('\n');
  final codeLines = lines.length > 1
      ? lines.sublist(1, lines.length - 1).join('\n')
      : '';
  final fenceRe = RegExp(r'^(```|~~~)(\w*)');
  final fMatch = fenceRe.firstMatch(lines.first);
  final lang = fMatch?.group(2) ?? '';
  if (codeLines.isNotEmpty) {
    widgets.add(
      Positioned(
        left: 8,
        top: topOffset + 2,
        right: 8,
        child: CodeBlockRenderer(
          code: codeLines,
          language: lang,
          maxWidth: maxWidth,
        ),
      ),
    );
  }
  break;
```

- [ ] **Step 4: 运行测试验证未回归**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/ -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): add code block fence detection and syntax-highlighted overlay

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 4 — 表格渲染

### Task 10: TableRenderer — 表格 Widget

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/rendering/table_renderer.dart`
- Test: `test/mindmap/ui/cards/live_preview/table_renderer_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/table_renderer_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/rendering/table_renderer.dart';

void main() {
  group('TableRenderer', () {
    testWidgets('renders simple table', (tester) async {
      final header = ['Name', 'Age', 'City'];
      final rows = [
        ['Alice', '30', 'NYC'],
      ];
      final aligns = [TextAlign.left, TextAlign.right, TextAlign.center];
      await tester.pumpWidget(
        MaterialApp(
          home: TableRenderer(
            headers: header,
            rows: rows,
            columnAligns: aligns,
            maxWidth: 400,
          ),
        ),
      );
      await tester.pump();
      expect(find.text('Name'), findsOneWidget);
      expect(find.text('Alice'), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/table_renderer_test.dart -v
```
Expected: FAIL

- [ ] **Step 3: 实现 TableRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/rendering/table_renderer.dart
import 'package:flutter/material.dart';

/// 渲染 markdown 管道表格的 widget。
///
/// 支持表头加粗 + 底部分隔线 + 列对齐。
class TableRenderer extends StatelessWidget {
  final List<String> headers;
  final List<List<String>> rows;
  final List<TextAlign> columnAligns;
  final double maxWidth;

  const TableRenderer({
    super.key,
    required this.headers,
    required this.rows,
    required this.columnAligns,
    required this.maxWidth,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: maxWidth,
      margin: const EdgeInsets.symmetric(vertical: 4),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(4),
        border: Border.all(color: const Color(0x33FFFFFF)),
      ),
      child: SingleChildScrollView(
        scrollDirection: Axis.horizontal,
        child: Table(
          defaultColumnWidth: const IntrinsicColumnWidth(),
          border: TableBorder.all(
            color: const Color(0x33FFFFFF),
            width: 0.5,
          ),
          children: [
            // 表头行
            TableRow(
              decoration: const BoxDecoration(
                color: Color(0x0FFFFFFF),
              ),
              children: List.generate(
                headers.length,
                (i) => _cell(headers[i], columnAligns[i], isHeader: true),
              ),
            ),
            // 数据行
            for (final row in rows)
              TableRow(
                children: List.generate(
                  row.length,
                  (i) => _cell(row[i], columnAligns[i]),
                ),
              ),
          ],
        ),
      ),
    );
  }

  Widget _cell(String text, TextAlign align, {bool isHeader = false}) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
      child: Text(
        text,
        style: TextStyle(
          fontSize: 13,
          fontWeight: isHeader ? FontWeight.w700 : FontWeight.w400,
          color: const Color(0xFFE6EDF3),
        ),
        textAlign: align,
      ),
    );
  }
}

/// 解析 markdown 管道表格字符串。
class MarkdownTableParser {
  /// 将管道表格文本解析为 headers、rows 和对齐方式。
  ///
  /// 输入示例:
  /// ```
  /// | Name | Age | City |
  /// |------|-----|------|
  /// | Alice | 30 | NYC |
  /// ```
  static TableParseResult parse(String tableText) {
    final lines = tableText
        .split('\n')
        .map((l) => l.trim())
        .where((l) => l.startsWith('|') && l.endsWith('|'))
        .toList();

    if (lines.length < 2) return TableParseResult.empty();

    final header = _splitRow(lines[0]);
    final alignLine = lines[1];
    final aligns = _parseAligns(alignLine, header.length);

    final rows = <List<String>>[];
    for (var i = 2; i < lines.length; i++) {
      final cells = _splitRow(lines[i]);
      if (cells.isNotEmpty && cells.length == header.length) {
        rows.add(cells);
      }
    }

    return TableParseResult(
      headers: header,
      rows: rows,
      columnAligns: aligns,
    );
  }

  static List<String> _splitRow(String line) {
    // 去掉首尾 | 后按 | 分割
    final content = line.substring(1, line.length - 1);
    return content.split('|').map((s) => s.trim()).toList();
  }

  static List<TextAlign> _parseAligns(String alignLine, int count) {
    final cells = _splitRow(alignLine);
    return List.generate(
      count,
      (i) {
        if (i >= cells.length) return TextAlign.left;
        final cell = cells[i].trim();
        if (cell.startsWith(':') && cell.endsWith(':')) {
          return TextAlign.center;
        } else if (cell.endsWith(':')) {
          return TextAlign.right;
        }
        return TextAlign.left;
      },
    );
  }
}

class TableParseResult {
  final List<String> headers;
  final List<List<String>> rows;
  final List<TextAlign> columnAligns;

  const TableParseResult({
    required this.headers,
    required this.rows,
    required this.columnAligns,
  });

  bool get isEmpty => headers.isEmpty;

  static TableParseResult empty() => const TableParseResult(
        headers: [],
        rows: [],
        columnAligns: [],
      );
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/table_renderer_test.dart -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/rendering/table_renderer.dart test/mindmap/ui/cards/live_preview/table_renderer_test.dart && git commit -m "$(cat <<'EOF'
feat(mindmap): add TableRenderer for pipe table rendering

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: LivePreviewController 表格占位符 + Overlay 集成

- [ ] **Step 1: 修改 PlaceholderTracker — 支持表格块检测**

添加表格占位符扫描方法：

```dart
/// 扫描 markdown 文本中管道表格块。
List<PlaceholderInfo> scanTables(String markdown) {
  final tables = <PlaceholderInfo>[];
  final lines = markdown.split('\n');
  var inTable = false;
  var tableStartLine = 0;
  var tableStartOffset = 0;
  var tableLines = <String>[];
  var lineOffset = 0;

  for (var i = 0; i < lines.length; i++) {
    final isTableRow = lines[i].trimLeft().startsWith('|');
    if (isTableRow && !inTable) {
      inTable = true;
      tableStartLine = i;
      tableStartOffset = lineOffset;
      tableLines = [lines[i]];
    } else if (isTableRow && inTable) {
      tableLines.add(lines[i]);
    } else if (!isTableRow && inTable) {
      inTable = false;
      if (tableLines.length >= 2) {
        final tableText = tableLines.join('\n');
        tables.add(PlaceholderInfo(
          id: 'table_${tableStartLine}',
          lineIndex: tableStartLine,
          charOffset: tableStartOffset,
          length: lineOffset - tableStartOffset - 1,
          type: 'table',
          rawMarkdown: tableText,
        ));
      }
    }
    lineOffset += lines[i].length + 1;
  }
  return tables;
}
```

- [ ] **Step 2: 修改 CardOverlayRenderer — 支持表格渲染**

在 switch 中添加 `table` 分支：

```dart
case 'table':
  final result = MarkdownTableParser.parse(p.rawMarkdown);
  if (!result.isEmpty) {
    widgets.add(
      Positioned(
        left: 8,
        top: topOffset + 2,
        right: 8,
        child: TableRenderer(
          headers: result.headers,
          rows: result.rows,
          columnAligns: result.columnAligns,
          maxWidth: maxWidth,
        ),
      ),
    );
  }
  break;
```

- [ ] **Step 3: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/ -v
```
Expected: PASS

- [ ] **Step 4: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): add pipe table detection and rendered overlay

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 5 — 数学公式

### Task 12: MathRenderer — 公式 Widget

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/rendering/math_renderer.dart`
- Test: `test/mindmap/ui/cards/live_preview/math_renderer_test.dart`

- [ ] **Step 1: 编写测试**

```dart
// test/mindmap/ui/cards/live_preview/math_renderer_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:starmind/src/mindmap/ui/cards/live_preview/rendering/math_renderer.dart';

void main() {
  group('MathRenderer', () {
    testWidgets('renders block math display', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: MathRenderer(
            tex: r'E = mc^2',
            isBlock: true,
            maxWidth: 400,
          ),
        ),
      );
      await tester.pump();
      // flutter_math_fork 渲染了一个 widget
      expect(find.byType(MathRenderer), findsOneWidget);
    });

    testWidgets('renders inline math display', (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: MathRenderer(
            tex: r'\alpha + \beta',
            isBlock: false,
            maxWidth: 400,
          ),
        ),
      );
      await tester.pump();
      expect(find.byType(MathRenderer), findsOneWidget);
    });
  });
}
```

- [ ] **Step 2: 实现 MathRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/rendering/math_renderer.dart
import 'package:flutter/material.dart';
import 'package:flutter_math_fork/flutter_math.dart';

/// 渲染 LaTeX 数学公式的 widget。
///
/// 使用 [flutter_math_fork] 实现公式排版。
/// 行内公式用小字号内联，块级公式居中显示。
class MathRenderer extends StatelessWidget {
  final String tex;
  final bool isBlock;
  final double maxWidth;

  const MathRenderer({
    super.key,
    required this.tex,
    required this.isBlock,
    required this.maxWidth,
  });

  @override
  Widget build(BuildContext context) {
    if (isBlock) {
      return Container(
        width: maxWidth,
        margin: const EdgeInsets.symmetric(vertical: 4),
        padding: const EdgeInsets.all(12),
        decoration: BoxDecoration(
          color: const Color(0x0FFFFFFF),
          borderRadius: BorderRadius.circular(4),
        ),
        child: Center(
          child: Math.tex(
            tex,
            textStyle: const TextStyle(fontSize: 16),
          ),
        ),
      );
    }

    return Math.tex(
      tex,
      textStyle: const TextStyle(fontSize: 14),
    );
  }
}

/// 从 markdown 文本中提取 LaTeX 公式。
class MathExtractor {
  /// 提取行内公式 `$...$`（非 `$$`）。
  static List<MathMatch> extractInline(String text) {
    final matches = <MathMatch>[];
    final re = RegExp(r'(?<!\$)\$([^$\n]+?)\$(?!\$)');
    for (final m in re.allMatches(text)) {
      matches.add(MathMatch(
        tex: m.group(1)!,
        isBlock: false,
        startOffset: m.start,
        endOffset: m.end,
      ));
    }
    return matches;
  }

  /// 提取块级公式 `$$...$$`。
  static List<MathMatch> extractBlock(String text) {
    final matches = <MathMatch>[];
    final re = RegExp(r'\$\$([^$]+)\$\$');
    for (final m in re.allMatches(text)) {
      matches.add(MathMatch(
        tex: m.group(1)!,
        isBlock: true,
        startOffset: m.start,
        endOffset: m.end,
      ));
    }
    return matches;
  }
}

class MathMatch {
  final String tex;
  final bool isBlock;
  final int startOffset;
  final int endOffset;

  const MathMatch({
    required this.tex,
    required this.isBlock,
    required this.startOffset,
    required this.endOffset,
  });
}
```

- [ ] **Step 3: 运行测试验证通过**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/live_preview/math_renderer_test.dart -v
```
Expected: PASS

- [ ] **Step 4: 集成到 CardOverlayRenderer**

在 switch 中添加 `math` 分支：

```dart
case 'math':
  // 从 rawMarkdown 提取 $$...$$ 内容
  final mathRe = RegExp(r'\$\$([^$]+)\$\$');
  final mathM = mathRe.firstMatch(p.rawMarkdown);
  if (mathM != null) {
    widgets.add(
      Positioned(
        left: 8,
        top: topOffset + 2,
        right: 8,
        child: MathRenderer(
          tex: mathM.group(1)!,
          isBlock: true,
          maxWidth: maxWidth,
        ),
      ),
    );
  }
  break;
```

- [ ] **Step 5: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): add MathRenderer for LaTeX formula display

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Phase 6 — Mermaid 图表

### Task 13: 导入 Mermaid 引擎

- [ ] **Step 1: 复制 marktext-plus 的 mermaid 目录**

```bash
cp -r D:/package/markdown-reference/marktext-plus/code/lib/ui/editor/mermaid/ D:/starmind/lib/src/mindmap/ui/cards/live_preview/rendering/mermaid/
```

- [ ] **Step 2: 修复导入路径**

修改 `D:/starmind/lib/src/mindmap/ui/cards/live_preview/rendering/mermaid/mermaid.dart` 中的导入：

```dart
// 将所有相对导入改为完整包内路径
// import '../parser/mermaid_parser.dart';  →  import 'parser/mermaid_parser.dart';
```

- [ ] **Step 3: 验证 Mermaid 可编译**

```bash
cd D:/starmind && flutter analyze lib/src/mindmap/ui/cards/live_preview/rendering/mermaid/ 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
cd D:/starmind && git add lib/src/mindmap/ui/cards/live_preview/rendering/mermaid/ && git commit -m "$(cat <<'EOF'
feat(mindmap): import mermaid rendering engine from marktext-plus

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 14: MermaidRenderer + 集成

**Files:**
- Create: `lib/src/mindmap/ui/cards/live_preview/rendering/mermaid_renderer.dart`
- Modify: `lib/src/mindmap/ui/cards/live_preview/card_overlay_renderer.dart`

- [ ] **Step 1: 实现 MermaidRenderer**

```dart
// lib/src/mindmap/ui/cards/live_preview/rendering/mermaid_renderer.dart
import 'package:flutter/material.dart';
import 'mermaid/mermaid.dart' as mermaid;

/// 渲染 Mermaid 图表的 widget。
///
/// 委托给 marktext-plus 的 mermaid 引擎进行解析和绘制。
class MermaidRenderer extends StatelessWidget {
  final String definition;
  final double maxWidth;

  const MermaidRenderer({
    super.key,
    required this.definition,
    required this.maxWidth,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: maxWidth,
      margin: const EdgeInsets.symmetric(vertical: 4),
      padding: const EdgeInsets.all(8),
      decoration: BoxDecoration(
        color: const Color(0x0FFFFFFF),
        borderRadius: BorderRadius.circular(4),
        border: Border.all(color: const Color(0x1AFFFFFF)),
      ),
      child: mermaid.MermaidWidget(
        definition: definition,
        width: maxWidth - 16,
      ),
    );
  }
}
```

- [ ] **Step 2: 集成到 CardOverlayRenderer**

在 switch 中添加 `mermaid` 分支：

```dart
case 'mermaid':
  final lines = p.rawMarkdown.split('\n');
  final content = lines.length > 1
      ? lines.sublist(1, lines.length - 1).join('\n')
      : '';
  if (content.isNotEmpty) {
    widgets.add(
      Positioned(
        left: 8,
        top: topOffset + 2,
        right: 8,
        child: MermaidRenderer(
          definition: content,
          maxWidth: maxWidth,
        ),
      ),
    );
  }
  break;
```

- [ ] **Step 3: 运行测试验证无回归**

```bash
cd D:/starmind && flutter test test/mindmap/ui/cards/ -v
```
Expected: PASS

- [ ] **Step 4: Commit**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
feat(mindmap): add MermaidRenderer with marktext-plus mermaid engine integration

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## 收尾

### Task 15: 整体验证 + graphify

- [ ] **Step 1: 运行所有测试**

```bash
cd D:/starmind && flutter test -v
```
Expected: ALL PASS

- [ ] **Step 2: 运行静态分析**

```bash
cd D:/starmind && flutter analyze lib/src/mindmap/ui/cards/live_preview/
```
Expected: No issues found

- [ ] **Step 3: 更新 graphify-out**

```bash
cd D:/starmind && /graphify
```

- [ ] **Step 4: 提交收尾**

```bash
cd D:/starmind && git add -A && git commit -m "$(cat <<'EOF'
chore(v16.4): run tests, analyze, and update graphify-out

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```